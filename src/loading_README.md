To load the data into tables, use:

```bash
python -m src.sales.load_sales

python -m src.data_cleaning_logs.load_data_cleaning_logs

python -m src.distributions.load_distributions 

python -m src.inventory.load_inventory

python -m src.products.load_products
```